<?xml version="1.0" encoding="utf-8"?>
<!--
    Data extraction rules for Android 12+ (API 31+).
    Security policy: no sensitive data should be included in backups or device transfers.
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude all files from cloud backup -->
    </cloud-backup>
    <device-transfer>
        <!-- Exclude all files from device transfer -->
    </device-transfer>
</data-extraction-rules>
